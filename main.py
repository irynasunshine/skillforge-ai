from src.cli.commands import app  # Import the CLI src object from the commands module

if __name__ == "__main__":  # Run only when this file is executed directly (not imported)
    app()  # Start the CLI application
